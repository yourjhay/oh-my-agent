# PDD (Phase-Driven Development) Implementation Plan

**Status:** Completed (2026-05-04).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a token-efficient **PDD** skill (`skills/phase-driven-development/SKILL.md` + `skills/phase-driven-development/reference.md`) that orchestrates roadmap-first phase development per `docs/superpowers/specs/2026-05-04-pdd-skill-design.md`, including roadmap OK gate, Phase 0 brainstorming contract, per-phase superpowers flow, and mandatory roadmap status updates (G7). The **full roadmap markdown table** is **deferred** by the repo author inside `reference.md` (see spec Implementation note).

**Layout note:** Skill packages live under **`skills/`** (e.g. `skills/phase-driven-development/`); former top-level `pdd/` path removed.

**Architecture:** One orchestrator `SKILL.md` (short) plus `reference.md` (contracts, status transitions, copy-paste gates, placeholder for roadmap table, mid-phase scope). Optional per-phase todo files are **not** required by PDD.

**Tech Stack:** Markdown skill files only; repository `agents-rules`.

---

## File map

| File | Role |
|------|------|
| `docs/superpowers/specs/2026-05-04-pdd-skill-design.md` | Design spec (already exists; updated for approved status + template deferral) |
| `skills/phase-driven-development/SKILL.md` | PDD orchestration: when to use, flow, interop, G7 rules, pointer to reference |
| `skills/phase-driven-development/reference.md` | Phase 0 contract, status transitions, roadmap OK / plan approval text, roadmap doc contract + **template slot**, optional visuals, mid-phase scope + re-approve flow |
| `README.md` | Contents table + layout; **PDD** row → `skills/phase-driven-development/` |

---

### Task 1: Commit spec updates

**Files:**
- Modify: `docs/superpowers/specs/2026-05-04-pdd-skill-design.md`

- [x] **Step 1:** Verify the spec includes **Approved for implementation**, **Implementation note (template deferred)**, and adjusted **G2** / token-strategy bullets.

- [x] **Step 2:** Commit

```bash
git add docs/superpowers/specs/2026-05-04-pdd-skill-design.md
git commit -m "docs: approve PDD design spec, defer roadmap table to author"
```

---

### Task 2: Create `skills/phase-driven-development/SKILL.md`

**Files:**
- Create: `skills/phase-driven-development/SKILL.md`

- [x] **Step 1:** Add YAML frontmatter (`name`, `description` — when to use PDD: multi-phase / roadmap-first).

- [x] **Step 2:** Include sections (concise, ≤ ~150 lines total): **Overview** (PDD = roadmap first, then per phase); **When to use**; **State machine** (qualify → Phase 0 roadmap brainstorming → roadmap OK gate → for each phase: spec brainstorming → spec review → writing-plans → plan approval → implement → verify → merge → **update roadmap status** → next phase); **Skill interop** (table matching design spec); **Rules** (no whole-feature writing-plans after Phase 0; explicit roadmap OK; one phase at a time until merge; G7 mandatory); **Reference** pointer — “read `reference.md` for Phase 0 contract, status transitions, approval asks, roadmap document shape / template slot.”

- [x] **Step 3:** Commit

```bash
git add skills/phase-driven-development/SKILL.md
git commit -m "feat(pdd): add PDD orchestrator SKILL.md"
```

---

### Task 3: Create `skills/phase-driven-development/reference.md`

**Files:**
- Create: `skills/phase-driven-development/reference.md`

- [x] **Step 1:** **Phase 0 brainstorming contract** — bullet list the agent must follow: announce brainstorming for roadmap only; deliverable = `docs/roadmaps/<feature>-roadmap.md` only; do **not** invoke `writing-plans` for the entire feature; do **not** write Phase 1+ specs in this pass.

- [x] **Step 2:** **Roadmap OK gate** — verbatim ask line from spec.

- [x] **Step 3:** **Status vocabulary** — list: `planned`, `brainstorming`, `spec`, `plan`, `approved`, `in-review`, `merged`, `blocked` with one-line meanings.

- [x] **Step 4:** **Transition rules** — table or list: when spec file exists → `spec`; plan written → `plan`; user approved plan → `approved`; PR open → `in-review`; merged → `merged`; **after each merge** update row + `Updated` column; during phase keep row aligned.

- [x] **Step 5:** **Plan approval ask** — short template (scope bullets, risks, out of scope, yes/changes/no).

- [x] **Step 6:** **Roadmap template slot** — Explicit subsection: *Author replaces this section with the canonical roadmap table* — include a **minimal example** one-row table so structure is unambiguous; teams paste their full table when ready. **No** bare “TBD”.

- [x] **Step 7:** **Per-phase brainstorming preamble** — one short paragraph: full brainstorming for **this phase only**; optional visuals **only** if user requested; output spec path convention with `-phase<N>`.

- [x] **Step 8:** **Optional visuals** — path pattern `docs/roadmaps/<feature>/phase<N>-visual.<ext>` if requested.

- [x] **Step 9:** **Mid-phase scope** — classification table + re-approve flow (self-contained in `reference.md`).

- [x] **Step 10:** Commit

```bash
git add skills/phase-driven-development/reference.md
git commit -m "feat(pdd): add reference contracts and deferred template slot"
```

---

### Task 4: Document skill location (optional)

**Files:**
- Modify: `README.md`

- [x] **Step 1:** Add a short bullet under “Contents”: **PDD** → `skills/phase-driven-development/SKILL.md` (roadmap-first) + `reference.md`.

- [x] **Step 2:** Commit

```bash
git add README.md
git commit -m "docs: link PDD skill in README"
```

---

## Self-review (plan vs spec)

| Spec item | Task |
|-----------|------|
| G1–G7 | Task 2–3 |
| Roadmap OK gate | Task 3 |
| Phase 0 contract | Task 3 |
| No todo-file mandate | Task 2 (non-goals); Task 3 omits todos |
| Template deferred | Task 3 §6; spec Implementation note |
| Interop table | Task 2 |

**Placeholder scan:** No implementation tasks left as “fill in later” without a concrete minimal example or pointer.

---

## Execution handoff

Plan saved to `docs/superpowers/plans/2026-05-04-pdd-skill.md`.

**1. Subagent-driven (recommended)** — Fresh subagent per task, review between tasks.

**2. Inline execution** — Run tasks 1–4 in order in this session.

**Which approach?** Inline execution completed in commit `1732419`; layout: skills under `skills/` (see git history if `pdd/` was present).
